dist/
node_modules/
.DS_Store
Thumbs.db
*.log

# Source art staging folder at the repo root only. The leading slash matters:
# a bare `images/` would also exclude public/assets/images/, which ships.
/images/
